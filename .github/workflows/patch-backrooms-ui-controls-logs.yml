name: Disabled obsolete patch workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This old patch workflow is disabled. Use normal source commits instead."

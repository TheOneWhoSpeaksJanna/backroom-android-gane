# Backrooms Level Zero 3D Godot Alpha

The true 3D rebuild lives in `/godot`.

It contains a mesh-based 3D Backrooms Level 0 prototype with mobile touch controls, breaker objectives, a strange exit door, yellow hazmat first-person arms, and a black shadow entity.

The dedicated build workflow is:

```text
.github/workflows/godot-android-build.yml
```

Expected output:

```text
Builds/BackroomsLevelZero3D.apk
Source/BackroomsLevelZeroGodotProject.zip
```
